# Codex Gateway Control

## 表达语言

- 除源代码本身外，仓库中所有面向人阅读的内容统一使用中文：包括但不限于 UI 文案、代码注释、README 与其他文档、脚本与命令行提示、错误提示、测试说明、配置文件注释和示例说明。
- 代码中的标识符、文件名、协议字段、HTTP 路径、命令、环境变量、第三方库/API 名称及其他必须与外部系统精确匹配的字面量可保留英文；不得为了汉化而破坏兼容性。
- 用户输入、上游服务返回的原文、密钥或其他不应改写的数据保持原样；需要补充解释时，以中文另行说明。

## 技术栈

- 运行时固定为 Node.js 22+ 与 TypeScript。
- Controller 使用 Express 5；SQLite 使用 Node 内置 `node:sqlite`。不引入 Docker、LiteLLM、Postgres 或远程数据库。
- Web 使用 React 19、Vite 6、Tailwind CSS v4（`@tailwindcss/vite`）和 shadcn/ui。
- 通过 shadcn CLI 添加 `Dialog`、`Popover`、`Command`、`Select` 等组件；其内部的 Radix 依赖由 shadcn 管理，不在业务组件中直接拼装 Radix 原语。图标使用 `lucide-react`。
- 不引入 Ant Design、MUI 或其他完整 UI 设计系统，也不平行维护第二套组件库。

## UI 规范

- 新 UI 优先使用 Tailwind utility；仅把 shadcn token、跨组件动画或无法表达的交互细节放入 `src/web/styles.css`。
- 优先通过 shadcn CLI 添加并复用现成组件，避免重复造轮子。`Button`、`Input`、`Textarea`、`Dialog`、`Popover`、`Command`、`Select`、`Tooltip`、`Tabs`、`Badge` 等基础交互不得自行重写，除非 shadcn 无法满足明确的业务需求。
- 需要自定义时，先组合 shadcn 组件并保留其可访问性行为；只为真实业务差异增加薄层封装，不创建平行的基础组件库。
- 模型选择必须使用 shadcn `Popover` + `Command` 构建可搜索、限高滚动列表，禁止使用展开全部选项的原生 `<select>`。
- 模态框使用 shadcn `Dialog`。底部命令区固定，中间内容独立滚动。
- 使用语义化按钮、输入、标签与可访问名称；不要用文字矩形替代已有的通用图标。
- 管理台保持紧凑、安静、便于扫描。页面区块不套浮动卡片；卡片只用于独立中转条目或模态工具。独立信息卡可使用 12px 圆角，路由主区和模态框可使用 16px 圆角，常规按钮和输入控件使用约 10px 圆角，避免生硬直角。

## 配色

采用“冷灰画布 + 靛蓝操作色”作为基础调色板：

- 画布/弱背景：`#f4f6fb`、`#eef1f7`
- 内容表面：`#ffffff`，仅用于卡片、输入和弹出层，不能形成突兀的大块底栏。
- 边框/次级强调：`#e2e6f0`、`#e7e9ff`
- 主操作/焦点：`#5b5cf0`
- 成功/状态强调：`#24946b`、`#eaf8f1`
- 危险操作背景：`#db5b74`

界面应以冷灰、白色与靛蓝为主，并以少量蓝绿、珊瑚状态色辅助扫描。深色终端仅用于真实流式输出；不要将渐变、圆形光斑或营销式大横幅用作装饰。

## 架构与安全边界

- Controller 是唯一控制面，也是唯一可以读取 SQLite、上游 API Key 与本地访问令牌的进程。
- API Key、Gateway access token、会话令牌和其他认证材料绝不能返回给 Web、iframe、CDP 注入脚本或日志。
- `/v1/*` 必须保持透明代理：不改写路径、模型名、tools、消息或 SSE 响应。已开始的流不可迁移。
- 管理页测试是单独的 Controller 接口：只向浏览器发送模型文本增量，不能泄露原始上游事件或密钥。
- Controller 与 Gateway 仅监听 `127.0.0.1`；所有管理写操作必须校验本地 `HttpOnly` 会话令牌。
- CDP 只做 Codex UI 嵌入，不能启动服务、保存密钥或调用管理 API。

## 验证

- 任何源码改动后至少运行 `npm run build` 和 `git diff --check`。
- 改动流式链路时，使用本地 mock 验证首个 SSE 文本增量在流结束前可见，并验证最终文本不重复。
- 改动管理页时，检查桌面与窄屏布局；文本不能溢出、遮挡或令底部操作不可达。
- 不要删除或覆盖用户已有的 `.env`、SQLite 数据、中转配置或无关工作区变更。


## 注释要求

所有代码都必须满足可维护性要求：

- 每个方法必须编写方法级注释。
- 注释必须说明：作用、参数、返回值、必要说明、异常或边界条件。
- 注释语言统一使用中文；专业术语、专有名词、协议名、类库名可保留英文。
- 方法体内无需逐行注释，但关键逻辑、主要流程、复杂处理必须有适当说明。
- 严禁只有空泛描述，例如“处理数据”“执行逻辑”等无效注释。
- 严禁无故删除已有注释。
- 任何重构、类型迁移、批量替换、脚本生成或 AI 自动修改，都不得删除、弱化或跳过已有注释；若签名、字段或语义变化导致注释需要更新，必须同步保留并改写，不能直接删掉。
